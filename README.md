# 🕐 Сортировка транзакций по дате

## Задача

Реализуйте функцию `sort_transactions()`, которая принимает список банковских транзакций и возвращает **названия операций, отсортированные от самых новых к самым старым**.

Транзакции содержат время в **разных форматах на русском языке**:

| Формат | Пример | Особенности |
|--------|--------|-------------|
| Полная дата с годом | `'22 фев 2022, 9:12'` | Год и время указаны явно |
| Дата без года | `'05 января, 12:34'` | Год = 2026 (текущий) |
| С пометкой "г." | `'12 апреля 2024 г.'` | Нужно игнорировать "г." |
| Сегодня с временем | `'Сегодня, 16:05 • PUSH'` | Время после запятой, лишний текст после • |
| Относительные даты | `'СЕГОДНЯ'`, `'ВЧЕРА'` | Без времени (считать 00:00) |
| Прошлый месяц | `'В ПРОШЛОМ МЕСЯЦЕ'` | Первый день предыдущего месяца |

### Требования к реализации

1. **Функция должна называться `sort_transactions`** и принимать один аргумент:
   ```python
   def sort_transactions(transactions: List[Dict[str, str]]) -> List[str]:
   ```
2. Для дат **без года** используйте **2026 год**
3. "Сегодня" в тестах = **11 февраля 2026 г.**
4. Сортировка: **от новых к старым** (новейшие — первыми в списке)
5. Обрабатывайте **любой регистр букв** (СЕГОДНЯ = сегодня = Сегодня)
6. Игнорируйте лишние символы: `•`, `PUSH`, `г.`, лишние пробелы

### Пример использования

```python
from task import sort_transactions

data = [
    {"operation": "Покупка акций 'Яндекс'", 'time': '22 фев 2022, 9:12'},
    {"operation": "Покупка акций 'Аэрофлот'", 'time': 'Сегодня, 16:05 • PUSH'},
    {"operation": "Покупка акций 'Северсталь'", 'time': 'ВЧЕРА'}
]

result = sort_transactions(data)
print(result)
# Вывод (пример):
# [
#   "Покупка акций 'Аэрофлот'",   # Сегодня 16:05 — самая новая
#   "Покупка акций 'Северсталь'",  # Вчера — средняя
#   "Покупка акций 'Яндекс'"       # 2022 год — самая старая
# ]
```

### Советы по реализации

✅ **Рекомендуется использовать:**
- Словарь для хранения месяцев (`{'январь': 1, 'февраль': 2, ...}`)
- Регулярные выражения для извлечения компонентов даты
- `datetime` для сравнения дат
- Блоки `try/except` для обработки некорректных данных

⚠️ **Избегайте:**
- Цепочек `if/elif` для проверки месяцев (лучше словарь)
- "Магических" строк и чисел в коде
- Дублирования логики парсинга

---

## 🧪 Автопроверка

Ваше решение будет автоматически проверено по критериям:

| Категория | Баллы | Что проверяется |
|-----------|-------|-----------------|
| **Функциональность** | 60 | Корректность сортировки на 5 наборах данных |
| **Качество кода** | 40 | Сигнатура функции, стиль, обработка ошибок, отсутствие дублирования |
| **Итого** | 100 | Проходной балл: **70** |

Результаты проверки будут отображены в **GitHub Actions Summary** с детализацией по каждому критерию.

---

## 📁 Структура проекта

```
├── task.py               ← ВАШЕ РЕШЕНИЕ (реализуйте функцию здесь)
├── tools/                ← Инструменты автопроверки (не изменять)
├── test_data.py          ← Тестовые данные (для ознакомления)
└── README.md             ← Это описание
```

**ВАЖНО:** Изменяйте **только файл `task.py`**. Все остальные файлы используются для автопроверки.
```

---
