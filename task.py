"""
Сортировка транзакций по дате проведения

ЗАДАЧА:
Реализуйте функцию sort_transactions(), которая принимает список транзакций
и возвращает названия операций, отсортированные от самых новых к самым старым.

Поддерживаемые форматы времени:
- '22 фев 2022, 9:12'        → полная дата с годом и временем
- '05 января, 12:34'         → дата без года (берётся текущий 2026)
- '12 апреля 2024 г.'        → дата с пометкой "г."
- 'Сегодня, 16:05 • PUSH'   → сегодня с временем и дополнительным текстом
- 'СЕГОДНЯ', 'ВЧЕРА'         → относительные даты без времени
- 'В ПРОШЛОМ МЕСЯЦЕ'         → первый день предыдущего месяца

ВАЖНО:
- Для дат без года используйте 2026 год
- "Сегодня" = 11 февраля 2026 г. (для тестов фиксируется)
- Сортировка: от новых к старым (новейшие — первыми в списке)
- Обрабатывайте разный регистр букв и лишние символы
"""

from datetime import datetime
from typing import List, Dict


def sort_transactions(transactions: List[Dict[str, str]]) -> List[str]:
    """
    Сортирует транзакции по дате от новых к старым.
    
    Args:
        transactions: Список словарей с ключами 'operation' и 'time'
    
    Returns:
        Список названий операций, отсортированный от новых к старым
    """
    # ВАШ КОД ЗДЕСЬ
    # 1. Распарсите каждую строку времени в объект datetime
    # 2. Отсортируйте транзакции по дате (новые → старые)
    # 3. Верните только названия операций
    raise NotImplementedError("Реализуйте функцию sort_transactions")